[project]
name = "accounts"
version = "0.1.0"
description = "User accounts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
